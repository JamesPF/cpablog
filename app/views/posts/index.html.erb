<!DOCTYPE <html>
<head>
  <title>Blog Posts</title>
  <link rel="stylesheet" type="text/css" href="style.css">
</head>
<body>
  <div class="admin-header">
    <p><a href="#">Logout</a></p>
    <img src="images/michael_felz_pc_med.png" height="150" width="401">
    <div class="admin-menu">
      <ul>
        <%= link_to posts_path do %><li>Posts</li><% end %>
        <%= link_to bios_path do %><li>About</li><% end %>
        <%= link_to settings_path do %><li>Settings</li><% end %>
      </ul>
    </div>
  </div>
  <div class="posts-container">
    <h3 id="new-post"><%= link_to 'New Post', new_post_path %></h3>
    <div class="posts">
      <h1>Manage Your Blog Posts</h1>
      <table>
        <thead>
          <% @post.each do |post|  %>
            <tr>
              <td><%= post.date %></td>
              <td><%= post.title %></td>
              <td><%= link_to 'Preview', post_path(post) %></td>
              <td><%= link_to 'Edit', edit_post_path(post) %></td>
              <td><%= link_to 'Delete', delete_post_path(post), method: :delete, data: {confirm: 'Are you sure?'} %></td>
            </tr>
          <% end %>
      </table>
    </div>
  </div>
</body>
</html>